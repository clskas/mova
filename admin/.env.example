# Passerelle API microservices (pas les services individuels)
NEXT_PUBLIC_API_URL=http://localhost:3000
# Laissé vide en prod — l'admin saisit son +243
NEXT_PUBLIC_ADMIN_PHONE=
# Client OAuth Web — Google staff (e-mail autorisé uniquement, pas n'importe quel Gmail)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
# Local demo without API only. Never set on Render (unset = real API).
# NEXT_PUBLIC_USE_API_MOCK=true
