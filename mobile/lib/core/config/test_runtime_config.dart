/// Désactivé en `flutter test` (voir `test/flutter_test_config.dart`).
bool movaMapTilesEnabled = true;

/// Évite l'appel Geolocator au montage de BookingScreen pendant les tests widget.
bool movaDisableAutoGps = false;

/// Évite la navigation auto vers TrackingScreen en mode mock (timer 1s).
bool movaSkipMatchingAutoTracking = false;
