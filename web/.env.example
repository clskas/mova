# Passerelle API microservices (pas les services individuels)
NEXT_PUBLIC_API_URL=http://localhost:3000
# Client OAuth Web (GIS). Même valeur que GOOGLE_CLIENT_ID côté mova-auth.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
